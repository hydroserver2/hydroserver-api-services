describe('Signup Page', () => {
  beforeEach(() => {
    cy.viewport(1500, 1200)
  })

  // it('Renders page', () => {})
  // it('error state', () => {})
  // it('successfully creates a user and logs them in', () => {})
})
